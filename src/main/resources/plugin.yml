name: RadarX
version: '${project.version}'
main: com.radarx.RadarXPlugin
api-version: 1.13
authors: [RadarX]
description: High-performance PlaceholderAPI distance & directional radar indicators
depend: [PlaceholderAPI]

commands:
  radarx:
    description: Main command for RadarX
    aliases: [radar, radarx]
    permission: radarx.admin
